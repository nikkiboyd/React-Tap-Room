import React from "react";

function EditPint() {
    var button = {
        backgroundColor: '#FFD24F',
    }

    return (
        <div>
            <button style={button} className="btn">Edit Details</button>
        </div>
    );
}

export default EditPint